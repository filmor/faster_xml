{plugins, [
    {rebar3_cargo,
        {git, "https://github.com/filmor/rebar3_cargo.git", {branch, "master"}}
    }
]}.

{provider_hooks, [
    {pre, [{compile, {cargo, build}}]},
    {post, [
        {clean, {cargo, clean}},
        {eunit, {cargo, test}}
    ]}
]}.

{deps, []}.
