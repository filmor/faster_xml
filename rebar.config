{plugins, [
    {rebar3_cargo,
        {git, "https://github.com/filmor/rebar3_cargo", {branch, "update"}}
    }
]}.

{provider_hooks, [
    {pre, [{compile, {rust, build}}]},
    {post, [
        {clean, {rust, clean}},
        {eunit, {rust, test}}
    ]}
]}.

{deps, []}.
